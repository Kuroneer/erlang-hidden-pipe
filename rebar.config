{erl_opts, [debug_info]}.
{deps, []}.

{provider_hooks, [{pre, [{ct, xref}]}]}.

{xref_checks, [
    undefined_function_calls,
    locals_not_used,
    undefined_functions
]}.

{profiles, [
    {test, [
        {erl_opts, [nowarn_export_all]}
    ]}
]}.

